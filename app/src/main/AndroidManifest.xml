<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Taller2"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDq26MfSkXmyhs74Cl8k_m2atMO-BAWG8E" />

        <!-- Splash Screen -->
        <activity android:name=".activities.Splash_Activity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->
        <activity android:name=".activities.MainActivity" android:exported="false"/>

        <!--  Activity -->
        <activity android:name=".activities.IndexActivity" android:exported="false"/>
        <!-- Login Activity-->
        <activity android:name=".activities.LoginActivity" android:exported="false"/>
        <!-- Resgister Activity-->
        <activity android:name=".activities.RegisterActivity" android:exported="false"/>
        <!-- Recuperacion Contrasena Activity-->
        <activity android:name=".activities.RecuperarContrasena" android:exported="false"/>
        <!-- Profile -->
        <activity android:name=".activities.Profile" android:exported="false"/>
        <!-- central -->
        <activity android:name=".activities.CentralActivity" android:exported="false"/>

    </application>


</manifest>
